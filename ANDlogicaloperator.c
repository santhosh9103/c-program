#include<stdio.h>
int main(){
    printf("result of the AND operator is %d",1&&0);
    printf("\n result of the OR operator id %d",0||5);
    return 0;
}
//&& the double ans percent symbol  is known as AND logical operator
//its function(The '&&' operator returns true when both the conditions under consideration are satisfied. Otherwise it returns false.)