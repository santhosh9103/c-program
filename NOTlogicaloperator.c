#include<stdio.h>
int main(){
    printf("enter the value of the NOT logical operator %d",!1);
    return 0;
}
//The logical-OR operator performs an inclusive-OR operation on its operands.
// The result is 0 if both operands have 0 values. If either operand has a nonzero value, the result is 1. 
//If the first operand of a logical-OR operation has a nonzero value, the second operand is not evaluated.